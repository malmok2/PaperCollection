# Codex 인수인계본 점검 결과 (2026-09-23)

## 결론

자동화가 안 된 근본 원인은 **실행 구조**입니다. 수집·가공 코드 자체는 대체로 동작했지만,
전체 흐름이 "연구실 Windows PC + Codex 전용 도구 3가지"에 동시에 의존해 어느 하나만 빠져도 멈추는 구조였습니다.

| # | 문제 | 근거 | 영향 |
|---|---|---|---|
| 1 | Windows 예약 작업이 **등록된 적 없음** | 인수인계 문서 §10, `logs/`에 09-21 로그 없음 | 09-21 회차 누락 |
| 2 | 예약 작업이 `LogonType Interactive` | `install_weekly_task.ps1` | PC가 꺼져 있거나 로그아웃 상태면 실행 안 됨 |
| 3 | 수집(08:00, 로컬)과 발송(09:00, Codex heartbeat)이 **분리** | 인수인계 §10 | 한쪽만 실패해도 메일 없음, 원인 추적 어려움 |
| 4 | Excel 생성이 `@oai/artifact-tool` (OpenAI 내부 패키지) 의존 | `build_literature_workbook.mjs` 2행 | Codex 밖에서는 실행 불가 |
| 5 | 대시보드 호스팅이 `*.chatgpt.site` (Codex Sites 도구) | `.openai/hosting.json` | Claude/일반 환경에서 갱신 불가 |
| 6 | 메일 발송이 Codex의 Gmail 도구 | 인수인계 §9 | 발송 코드·인증 정보가 저장소에 없음 |
| 7 | 절대경로 하드코딩 (`C:\Users\Idaho_S\...`, Python/Node 경로) | 5개 파일 | 다른 PC·클라우드에서 실행 불가 |
| 8 | 09-14 회차도 실제로는 **09-18에 수동 실행** | `latest-run.json` started_at | 월요일 자동 실행이 한 번도 성공 못 함 |
| 9 | 비교기간 수집에서 Crossref 400 오류 이력 | `weekly-pipeline-2026-09-14.log` (존재하지 않는 `subtype` select 필드) | 이후 수정됨. 재시도 로직 없음 |

## 데이터 품질 문제

| 항목 | 현황 | 조치 |
|---|---|---|
| 초록 | 344편 중 **0편** (Crossref에 Elsevier 초록 없음) | OpenAlex 초록 보강 추가 |
| 한글 제목 | MyMemory 번역 오류 다수: thermal-hydraulic→"열유압", pebble bed→"조약돌층", multiphysics→"다중 물리학", "¶" 혼입 | 전문용어 용어집 기반 재번역, 이후 Claude API 번역 |
| Open Access | Elsevier API(키 없음, 학내 IP 인증 추정)에 의존 | OpenAlex `is_oa`로 대체 |
| 토픽 규칙 | 3개 파일에 중복 정의 | `config/settings.json`으로 통합 |
| 대시보드 문구 | "최근 2회 수집분" 하드코딩 (실제 4회) | 실제 회차 수로 표시 |
| 소수 논문 주간 | KMeans 7군집 고정 → 논문 수가 적거나 제목이 비슷하면 오류 | 군집 수 자동 조정, 빈 군집 처리 |

## 이전 후 구조

- 실행: GitHub Actions (클라우드, 월 07:00 KST + 12:00 재시도), PC 불필요
- 수집→가공→발송을 **한 작업**에서 수행, 실패 시 실패 알림 메일
- 누락 주 자동 backfill, 같은 주 중복 발송 방지(`state/sent-runs.json`)
- DB·결과물은 매주 저장소에 커밋 → git 이력이 회차별 백업
- Claude 원격 세션 환경은 네트워크 정책상 Crossref/OpenAlex 접속이 차단되어(403) 수집 실행 환경으로 부적합 → GitHub Actions 선택
